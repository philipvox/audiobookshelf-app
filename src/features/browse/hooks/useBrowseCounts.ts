/**
 * src/features/browse/hooks/useBrowseCounts.ts
 *
 * Re-export from shared location for backwards compatibility.
 * The actual hook now lives at src/shared/hooks/useBrowseCounts.ts
 */

export { useBrowseCounts, DURATION_RANGES } from '@/shared/hooks/useBrowseCounts';
export type { BrowseCounts, DurationRangeId } from '@/shared/hooks/useBrowseCounts';
